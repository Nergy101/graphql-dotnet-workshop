query Books {
  books {
    nodes {
      id
      title
      createdAt
    }
  }
}

query BooksSorted($sorting: [BookSortInput!]) {
  books2(order: $sorting) {
    nodes {
      id
      title
      createdAt
    }
  }
}
